# Backend API Server Dependencies (lightweight)
fastapi==0.109.0
uvicorn==0.27.0
python-jose[cryptography]==3.3.0
python-dotenv==1.0.0
pydantic==2.5.0
python-multipart==0.0.6
requests==2.31.0
httpx>=0.25.0 # Required for anthropic SDK (socket_options support)

# Database
sqlite-utils==3.36
cachetools>=5.3.0  # For TTL caching in feed algorithm

# Vector search (needed for semantic_db.search_insights)
numpy<2.0.0  # ChromaDB needs NumPy 1.x
chromadb==0.4.22  # Latest stable 0.4.x, matches your original >=0.4.0
sentence-transformers>=2.7.0  # Compatible with modern huggingface_hub

# SLM API (needed for semantic_db evaluation)
groq>=0.11.0

# Local SLM for topic validation
transformers>=4.40.0
torch>=2.0.0
sentencepiece>=0.1.99  # For Llama tokenizer
accelerate>=0.20.0  # For optimized model loading

# Automation/Extraction dependencies (lazy loaded when extraction runs)
anthropic>=0.18.0  # Claude API for query generation and extraction
ddgs>=4.0.0  # Source discovery
crawl4ai>=0.3.0  # Web content fetching
